{% extends "adminBundle::layout.html.twig" %}

{% block title %}Application Cinema{% endblock %}

{% block body %}
<div class="container">
    <div class="container-fluid">
        <h2>Ajouter un cinéma</h2>

        {{ form_start(form) }}
            <div class="form-group">
                <label for="">{{ form_label(form.nom) }}</label>
                {{ form_widget(form.nom) }}
            </div>
            <br>
            <div class="form-group">
                <label for="">{{ form_label(form.adresse) }}</label>
                {{ form_widget(form.adresse) }}
            </div>
            <br>
            <div class="form-group">
                {{ form_widget(form.accessiblite) }}
            </div> 
        {{ form_end(form) }}

    </div>
</div>

{% endblock %}
